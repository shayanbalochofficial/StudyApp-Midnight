import React from "react";
import { motion } from "framer-motion";

const AnimatedContent = () => {
  return (
    <motion.div className="z-10 backdrop-blur-lg bg-black/30"></motion.div>
  );
};

export default AnimatedContent;
